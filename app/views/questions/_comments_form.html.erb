<%= form_for [@commentable, @comment] do |f| %>
  <div class="form-group">
  	<%= f.text_field :body, class: "form-control", placeholder: "Writte a comment" %>
  </div>

  <div>
    <%= f.submit "Comment", class: "btn btn-default" %>
  </div>
<% end %>